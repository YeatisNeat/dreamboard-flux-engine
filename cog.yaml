# cog.yaml
# Clean slate version, following official documentation exactly.
build:
  gpu: true
  python_version: "3.10"
  python_packages:
    - "torch==2.1.0"
    - "transformers==4.38.2"
    - "accelerate==0.27.2"
    - "sentencepiece"

predict: "predict.py:Predictor"
